
### Setup Authentik SSO — Direct Google OAuth
- Cek social source Google di [[Authentik_SSO]] → confirmed active
- Confirm URL direct ke Google: `https://auth.blitztechnology.tech/source/oauth/login/google/`
- Documented dual-button pattern: "Login with Authentik" + "Login with Google"
- User tetap ke-track di Authentik meski login via Google
- Semua dicatat di `[[04_INFRASTRUCTURE_REFERENCE/Authentik_SSO]]`

### Custom Login Authentik — CANCELLED, Revert ke Dex
- Attempt: Custom login page untuk Authentik SSO dengan Brajakara branding
- Approach coba: Option A (static HTML), Option C (template override), Option D (rebuild frontend)
- **Option D executed**: Clone Authentik `version/2024.10.1` → edit `custom.css` → rebuild frontend
- **Problem cascade**:
  - CSS override PatternFly terlalu aggressive → blank page
  - Minimal CSS override → warna berubah tapi layout kacau
  - Shadow DOM di web components prevent penetration
  - Redirect loop: `/` → flow → `/` lagi
  - Login sukses tapi redirect balik ke login page
- **Keputusan final**: CANCELLED — matikan Authentik, revert ke [[Dex_OIDC]]
- **Action taken**:
  - Stop Authentik containers (server, worker, redis, postgresql)
  - Start Dex kembali di port 5556
  - Update nginx azkaban: proxy ke `10.20.0.11:5556`
  - Revert Outline OIDC config ke Dex endpoints
  - Restart Outline container
- **Status**: Dex running, Outline OIDC stable
- **Lesson**: Authentik custom frontend butuh deeper knowledge, Dex lebih simple untuk use case ini
- Tracked di [[SOFTW-61]] (status: Cancelled)
