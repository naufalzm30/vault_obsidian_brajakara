---
date: 2026-05-05
tags: [daily-note]
---

# Daily Note — 2026-05-05

## Recap Hari Ini

### Dokumentasi Registry Docker
- Membuat catatan untuk Docker Registry Brajakara (`https://registry-ui.blitztechnology.tech/`).
- Rencana kedepannya untuk sentralisasi image Docker agar mempermudah deployment antar server.

↗ Masuk [[rekam_jejak]]

### Riset Setup Nginx Auth Proxy untuk Registry
- Audit teknologi registry di [[04_INFRASTRUCTURE_REFERENCE/Docker_Registry|Docker Registry]] tower: `registry:2.8`, backend MinIO, auth htpasswd 1 user saja
- Audit resource [[Proxmox_MORDOR|tower]]: 8GB RAM, 4 vCPU, 127GB disk free — Harbor terlalu berat
- Nginx systemd sudah running di tower, 2 vhost aktif (`default`, `pdam_test`), belum ada proxy untuk registry
- Arsitektur sekarang: registry port 5000 expose publik langsung, tidak ada gatekeeper
- Rencana: Nginx Auth Proxy — tetap pakai `registry:2.8`, user `builder` (push+pull) vs `prod` (pull only)

↗ Masuk [[rekam_jejak]]
