---
date: 2026-04-27
tags: [daily-note]
up: "[[00_INBOX/Daily_Notes/index]]"
---

# Daily Note — 2026-04-27

## Recap Hari Ini

### Fix MCP Plane Integration
- MCP Plane tool `mcp__plane__list_projects` gagal 404
- Root cause: workspace slug tidak di-pass correct dari env var ke API endpoint
- Fix: hardcode workspace `brajakara` di base URL di `~/.claude/settings.json`
- Test via curl: 7 projects detected (WEBAP, SOFTW, HARDW, PALEM, GIZPR, BSNS, BKS)
- Workaround documented kalau masih error: pakai `rtk proxy curl` langsung

↗ Update CLAUDE.md

### Improve CLAUDE.md Documentation
- Add vault scope clarification di Overview — vault adalah meta-layer, bukan code projects itu sendiri
- Add Multi-AI Support section — mention GEMINI.md untuk Gemini CLI users
- Add MCP Integrations section — document Plane workspace config + known issues + workaround

File: `CLAUDE.md` (3 new sections added)

↗ Masuk rekam_jejak

### Test MCP Plane Integration
- MCP `list_projects` + `get_me` tools masih 404 — routing issue di `plane-mcp-server` belum fix
- API key correct: `plane_api_955f282b8c8a40aab8fe93b5a6710893` (dari `~/.claude/settings.json`)
- Curl workaround kerja — 7 projects return: WEBAP, SOFTW, HARDW, PALEM, GIZPR, BSNS, BKS
- User coba run `uvx plane-mcp-server` manual → JSON-RPC parsing error (normal, karena `stdio` mode expect input dari Claude Code client, bukan manual terminal)
- Confirmed: Plane API endpoint accessible via `rtk proxy curl` + manual parse, MCP server construct URL salah
