---
date: 2026-04-21
tags: [daily-note]
---

# Daily Note — 2026-04-21

## Recap Hari Ini

### BRAJA_PDAMSBY — Integrasi Notif Telegram check_data
- Lanjut task pending dari 2026-04-20: integrasi Telegram ke `run_checks.py`
- Menunggu bot token dan list channel ID dari user
- Target: kirim notif otomatis ke channel Telegram jika ada station yang masuk kategori missing/threshold/voltased

### Fix Hook SessionStart Error
- Hook `SessionStart` dan `UserPromptSubmit` error karena `node` tidak ada di PATH saat hook jalan (non-interactive shell tidak load nvm)
- Buat `.claude/find-node.sh` — wrapper portabel yang deteksi node dari nvm, volta, fnm, atau system PATH
- Update `settings.json` untuk pakai wrapper tersebut di kedua hooks
- Push ke GitHub agar fix ini ter-sync ke semua mesin

### Setup Tower — RTK + code-review-graph MCP
- Install RTK 0.37.2 di tower (`~/.local/bin/rtk`) — sebelumnya cuma di salazar
- Register repo `BRAJA_PDAMSBY` ke code-review-graph MCP — graph: 1183 nodes, 9063 edges, 176 files
- Demo bukti efek RTK: `docker logs pdam_web --tail 50 | rtk log` → kompres jadi 4 baris summary (errors/warnings/info counts)

### Aturan WAJIB RTK + Caveman
- Tambah section `## WAJIB: RTK + Caveman` ke `CLAUDE.md` vault
- Sync ke 4 lokasi memory (feedback_behavior.md di vault & ~/.claude, Claude_Memory_Feedback.md, CLAUDE.md)
- Inti aturan: RTK wajib prefix di depan command verbose; hook `rtk hook claude` cuma telemetry — bukan auto-route. Caveman tetap jalan kecuali code/commit/security
