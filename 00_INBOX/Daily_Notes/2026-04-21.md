---
date: 2026-04-21
tags: [daily-note]
---

# Daily Note — 2026-04-21

## Recap Hari Ini

### PDAM_SBY — Integrasi Notif Telegram check_data
- Lanjut task pending dari 2026-04-20: integrasi Telegram ke `run_checks.py`
- Menunggu bot token dan list channel ID dari user
- Target: kirim notif otomatis ke channel Telegram jika ada station yang masuk kategori missing/threshold/voltased

### Fix Hook SessionStart Error
- Hook `SessionStart` dan `UserPromptSubmit` error karena `node` tidak ada di PATH saat hook jalan (non-interactive shell tidak load nvm)
- Buat `.claude/find-node.sh` — wrapper portabel yang deteksi node dari nvm, volta, fnm, atau system PATH
- Update `settings.json` untuk pakai wrapper tersebut di kedua hooks
- Push ke GitHub agar fix ini ter-sync ke semua mesin

### PDAM_SBY — Stateful Alert System + Telegram Integration
- Bot: @suryasembadabot (`7699190789:AAEfYfO4xDuzHQqOXrEz39R2JOTcJrKhSDs`)
- 4 check module: `check_missing`, `check_threshold` (status=1), `check_voltased` (<11V), `check_minus` (status=6, NEW)
- 4 channel Telegram: MISSING, THRESHOLD, VOLTASE_TURUN, KUBIKASI_MINUS
- Format notif: `🔴 Missing 31-10-2025 11:00 — 11:55 \n • alias(id): count`
- **Stateful alert system** via Redis:
  - `alert_state.py` — state per station: OK → SUSPECT → MISSING → CRITICAL
  - `alert_classifier.py` — 2-cycle confirm (SUSPECT→MISSING), escalate >2 jam → CRITICAL
  - `alert_router.py` — tier routing: WARNING real-time, CRITICAL always, INFO digest, ≥5 station = OUTAGE 1 notif
  - Graceful fallback: kalau Redis down → bypass state machine, langsung WARNING
- **Lagging window**: lag=20 mnt, window=60 mnt → alert ~45 mnt setelah data stop
- Crontab: `*/15 * * * *` di `pdam_scheduler`
- Quiet hours: 22:00–06:00 WARNING di-buffer, CRITICAL tetap real-time
- Prod server: Brajakara_Infrastructure_Overview (`/home/usflowmeter/pdam-daas-project/src/DOCKER_BRAJA_PDAMSBY`) — perlu deploy manual + tambah `TELEGRAM_BOT_TOKEN` ke `.env` prod

↗ Masuk rekam_jejak

### Setup Tower — RTK + code-review-graph MCP
- Install RTK 0.37.2 di tower (`~/.local/bin/rtk`) — sebelumnya cuma di salazar
- Register repo PDAM_SBY ke code-review-graph MCP — graph: 1183 nodes, 9063 edges, 176 files
- Demo bukti efek RTK: `docker logs pdam_web --tail 50 | rtk log` → kompres jadi 4 baris summary (errors/warnings/info counts)

### Aturan WAJIB RTK + Caveman
- Tambah section `## WAJIB: RTK + Caveman` ke `CLAUDE.md` vault
- Sync ke 4 lokasi memory (feedback_behavior.md di vault & ~/.claude, Claude_Memory_Feedback.md, CLAUDE.md)
- Inti aturan: RTK wajib prefix di depan command verbose; hook `rtk hook claude` cuma telemetry — bukan auto-route. Caveman tetap jalan kecuali code/commit/security
