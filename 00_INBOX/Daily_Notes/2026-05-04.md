---
date: 2026-05-04
tags: [daily-note]
---

# Daily Note — 2026-05-04

## Recap Hari Ini

### Cek Status Plane — DB_UAT Portable
- Review BRAJA_PDAMSBY issue SOFTW-59 — DB_UAT portable
- Bug blocker: `pdam_station` kosong di dev DB karena dump dibuat saat prod masih kosong
- Plan: re-export `02_account.dump` dan `03_static_data.dump` dari prod (fresh data)

### Update Konfirmasi Rule
- User tegur Claude sering lupa konfirmasi git state-changing commands (`checkout`, `stash`, `stash pop`)
- Update section "Konfirmasi Sebelum Eksekusi" di 4 tempat: CLAUDE.md, feedback_behavior.md, Claude_Memory_Feedback.md, feedback_notes.md
- Tambah kategori: File operations, System, **Git state-changing**, Development, Config
- Git read-only (status, log, diff, branch -a, show) tetap tidak perlu konfirmasi

### DB_UAT Installer — Plane-Style TUI
- Rewrite `install.sh` dengan aesthetic frame-based menu (centered, cyan boxes)
- Strategy: `box` header + verbose logging + spinners untuk setiap step
- Iterate 15+ kali: fix terminal leakage, clear screen timing, docker build output, centered text
- Final pattern: `run_step` + `clear` + `box` + piped output via `tee` (keeps frame stable)
- Fix path issue: `fetch_account_static.sh` dijalankan dari **host** (bukan docker) karena butuh SSH keys
- `fetch_from_prod.sh`, `fetch_active_records.sh`, `import_dynamic.sh` dijalankan dari **container** (docker exec)
- Status: **Paused** — tunggu user test ulang, script masih stuck waiting SSH tunnel

### DB_UAT Installer — Final Fixes & Testing
- Issue: Schema import error (dead table FK references) — fixed by removing `ALTER TABLE` + ACL untuk `sensor_data`, `checker_data`, `station` dari `01_schema.sql`
- Issue: Static data duplicate key — fixed by `TRUNCATE CASCADE` sebelum import
- Issue: `fzf` arrow key menu corrupt display saat script output streaming — reverted ke numeric input (stable)
- Issue: Script exit code 137 (SIGKILL) saat `fetch_account_static.sh` jalan — root cause `set -e` + `tee` pipe conflict, removed `set -e`
- Created `test_install.sh` — automated E2E test, streams output real-time, verify static + dynamic tables + container health
- Status: **Ready to test** — semua fix applied, tunggu full Fresh Install run

↗ Masuk [[rekam_jejak]]
