---
date: 2026-04-16
tags: [daily-note]
---

# Daily Note — 2026-04-16

## Recap Hari Ini

### Setup Vault Obsidian Brajakara
- Inisialisasi vault `vault_obsidian_brajakara` sebagai knowledge base backend
- Setup git remote ke `git@github.com:naufalzm30/vault_obsidian_brajakara.git`
- Konfigurasi `.gitignore` — exclude `settings.local.json` (machine-specific) dan `workspace.json`
- Aktifkan auto-permission untuk Bash/file tools tanpa prompt
- Tambah rule git sync otomatis di `CLAUDE.md`: pull → add → commit → push setiap ada perubahan
- Tambah `.gitkeep` di folder-folder kosong agar ter-track git

### Verifikasi
- Vault sudah sync dengan remote `origin/master` — up to date, working tree bersih
- Test daily note pertama berhasil dibuat

### Insiden: VM Tidak Bisa Konek ke Network
- **Root cause:** Nama interface salah — diasumsikan `enp0s16`, padahal aslinya `enp6s18`
- **Deteksi:** `ip link` output tunjukkan baris `2: enp6s18: <BROADCAST,MULTICAST,UP,LOWER_UP>`
- **Fix sementara (runtime):**
  ```bash
  ip addr add 192.168.1.100/24 dev enp6s18
  ip route add default via 192.168.1.1
  ```
- **Verifikasi:** `ip addr show enp6s18` → `inet 192.168.1.100/24 scope global enp6s18` ✓
- **Ping test:** `ping 8.8.8.8` berhasil (17.6ms), 75% packet loss awal tapi konek
- **Fix permanent via netplan** (`/etc/netplan/01-netcfg.yaml`):
  ```yaml
  network:
    version: 2
    ethernets:
      enp6s18:
        dhcp4: true
  ```
  lalu `netplan apply`

## Catatan
- Caveman Mode aktif (full) sejak awal session
- Interface VM: `enp6s18` (mac: `bc:24:11:79:83:95`), IP: `192.168.1.100/24`, GW: `192.168.1.1`
