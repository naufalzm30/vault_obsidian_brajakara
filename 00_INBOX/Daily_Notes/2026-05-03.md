---
date: 2026-05-03
tags: [daily-note]
---

# Daily Note — 2026-05-03

## Recap Hari Ini

### Pelajari Vault + Startup Session (pi)
- Session baru via **pi** (bukan Claude Code langsung) — pi baca `CLAUDE.md` otomatis (kompatibel tanpa perlu `AGENTS.md`)
- Startup ritual: git fetch + pull, baca vault dari Navigation_Map → semua folder index → detail files
- Context yang sudah dimuat: BE_WEATHERAPP, PDAM_SBY, weatherapp_mqtt_parser, infrastruktur (rockbottom, azkaban, riverstyx, FOEWS, MORDOR), semua VM, profil Naufal
- Memory live + mirror sudah sinkron
- **Task pending masih open:** PDAM check_data Telegram (token bot + channel ID belum diisi), MCP Plane self-hosted 404

### Build Plane pi Extension
- MCP npm (`makeplane/plane-mcp-server`) tidak support pi (pi no MCP) — rebuild sebagai **pi Extension** TypeScript
- Extension: `~/.pi/agent/extensions/plane/index.ts` — 5 tools: `plane_list_projects`, `plane_list_issues`, `plane_get_issue`, `plane_create_issue`, `plane_update_issue`
- Config: `~/.pi/agent/extensions/plane/config.json` (API key, base URL, workspace slug)
- Test API manual: projects OK (7 projects), issues WEBAP OK, states OK
- Extension load otomatis saat pi start (auto-discovery `~/.pi/agent/extensions/`)
- **Status: siap di-reload dan dicoba** — `/reload` di pi untuk load extension

↗ Masuk [[rekam_jejak]]
