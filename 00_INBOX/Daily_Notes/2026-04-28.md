---
date: 2026-04-28
tags: [daily-note]
---

# Daily Note — 2026-04-28

## Recap Hari Ini

### MCP [[Brajakara_Infrastructure_Overview|Plane]] Troubleshooting — 404 Debug
**Status:** Pending restart test

**Temuan:**
- MCP Plane tools (`mcp__plane__list_projects`) return HTTP 404
- Direct curl ke API work: `https://plane.blitztechnology.tech/api/v1/workspaces/brajakara/projects/` — return 7 projects (WEBAP, SOFTW, HARDW, PALEM, GIZPR, BSNS, BKS)
- **SDK test confirmed work** — `plane-sdk==0.2.10` berhasil fetch projects dari self-hosted instance via test script `/tmp/test_plane_sdk.py`
- Config MCP di `~/.claude/settings.json` sudah benar sesuai official repo:
  ```json
  {
    "command": "uvx",
    "args": ["plane-mcp-server", "stdio"],
    "env": {
      "PLANE_API_KEY": "plane_api_955f282b8c8a40aab8fe93b5a6710893",
      "PLANE_BASE_URL": "https://plane.blitztechnology.tech",
      "PLANE_WORKSPACE_SLUG": "brajakara"
    }
  }
  ```

**Root cause hypothesis:**
- Bukan bug di SDK atau base URL
- Kemungkinan MCP server instance di Claude Code cached/tidak reload config proper
- Atau env vars tidak passed ke MCP server subprocess

**Next action setelah restart:**
1. Test ulang `mcp__plane__list_projects` — verify apakah masih 404
2. Kalau masih error, check alternative:
   - Buat curl wrapper function untuk Plane API
   - Debug MCP server logs (kalau ada)
   - Report issue ke repo `makeplane/plane-mcp-server` tentang self-hosted troubleshooting

**Test script reference:** `/tmp/test_plane_sdk.py` (verified working)

### Update — MCP Plane Tools Not Available
- Session baru: MCP Plane tools tidak muncul di available functions
- Config di `~/.claude/settings.json` intact — `uvx plane-mcp-server stdio`
- `uvx` installed (0.11.7), tapi `plane-mcp-server` run on-demand
- **Workaround curl berhasil** — fetch 7 projects via `rtk proxy curl`
- Kemungkinan MCP server belum initialized atau butuh restart Claude Code

↗ Masuk [[rekam_jejak]]
